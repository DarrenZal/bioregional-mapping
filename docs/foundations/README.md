# docs/foundations/

Local foundation docs for bioregional-mapping.

**Current state (2026-05-04)**: zero local foundation docs admitted.

bioregional-coordination + Spore foundation docs are cited at upstream-reference layer (see `../../project-vision.md` §"Coordination substrate"). Local foundation docs admit here only when mapping-specific operational pressure surfaces that bioregional-coordination + Spore canon does not cover.

When admitting a local foundation doc:

- Frontmatter `doc_id: bioregional-mapping.<slug>`, `doc_kind: foundation`
- Cite upstream surfaces explicitly (`depends_on: [bioregional-coordination.<slug>, spore.<slug>, ...]`)
- Author with companion ADR in `../research/canon-decisions/` recording the admission decision
- Earning-test discipline applies: foundation-doc admission requires `ready_convergent` or `ready_with_tension` state from `convergence_export.py` per the Spore intake protocol convergence-readiness gate (≥2 source claims aligned, with documented opposition where present)

Candidate specializations surfaced at vision layer (NOT yet admitted as foundation docs): layer-as-membrane, Story-of-Place as Field manifestation, Workshop-as-Joint-Commitment, Field Guide as Reproduction artifact. See `../../project-vision.md` §"How upstream primitives specialize at mapping practice".
